@*@page "/"*@

@using System.Net.Http
@using System.Net.Http.Json
@using Microsoft.AspNetCore.Components.Forms
@using Microsoft.AspNetCore.Components.Routing
@using Microsoft.AspNetCore.Components.Web
@using Microsoft.AspNetCore.Components.Web.Virtualization
@using Microsoft.JSInterop


@*<CascadingAuthenticationState>*@
    <Router AppAssembly="@typeof(Index).Assembly">
        <Found >

        </Found>
        <NotFound>

        </NotFound>
@*        <Found Context="routeData">
            @*<AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)">
                <NotAuthorized>
                    @if (context.User.Identity?.IsAuthenticated != true)
                    {
                        <RedirectToLogin />
                    }
                    else
                    {
                        <p role="alert">You are not authorized to access this resource.</p>
                    }
                </NotAuthorized>
            </AuthorizeRouteView>
            <FocusOnNavigate RouteData="@routeData" Selector="h1" />
        </Found>
        <NotFound>
            <PageTitle>Not found</PageTitle>
            <LayoutView Layout="@typeof(MainWindow)">
                <p role="alert">Sorry, there's nothing at this address.</p>
            </LayoutView>
        </NotFound>*@
    </Router>
@*</CascadingAuthenticationState>*@

<StudentCMP EventCallBackProperty="ClickHandler"></StudentCMP>
EventCallBackProperty="ClickHandler"
@if(fistname==null)
{
    <p>first name is null</p>
}
else
{
    <p>@fistname</p>
}
@*<button onclick="@(()=>f1())"></button>
<button @onclick="(()=>f1())"></button>
*@
<input @bind="fistname" />



@code {

    string fistname = null;

    void f1(string x)
    {
        
    }
    void ClickHandler(StudentModel newMessage)
    {
        fistname = newMessage.FirstName;
    }

}