@page "/"
@inherits StudentCMPBase
@inject NavigationManager UriHelper

@*<button @onclick="@(()=>EventCallBackProperty.InvokeAsync(_childData))">Child Button</button>*@
<a href="/r1page">click</a>
<button @onclick=@Navigate>Navigate</button>

<EditForm Model="@StudentModelObj" OnValidSubmit="HandleValidSubmit">
    <DataAnnotationsValidator />
    <h3>Edit Employee</h3>
    <hr />
    <ValidationSummary />
    <div class="form-group row">
        <label for="firstName" class="col-sm-2 col-form-label">
            First Name
        </label>
        <div class="col-sm-10">
            <InputText id="firstName" class="form-control" placeholder="First Name"
                       @bind-Value="StudentModelObj.FirstName" />
            <ValidationMessage For="@(() => StudentModelObj.FirstName)" />
        </div>
    </div>
    <div class="form-group row">
        <label for="firstName" class="col-sm-2 col-form-label">
            Last Name
        </label>
        <div class="col-sm-10">
            <InputText id="firstName" class="form-control" placeholder="Last Name"
                       @bind-Value="StudentModelObj.LastName" />
            <ValidationMessage For="@(() => StudentModelObj.LastName)" />
        </div>
    </div>
    <div class="form-group row">
        <label for="firstName" class="col-sm-2 col-form-label">
            Age
        </label>
        <div class="col-sm-10">
            <InputNumber id="firstName" class="form-control"
                       @bind-Value="StudentModelObj.Age" />
            <ValidationMessage For="@(() => StudentModelObj.Age)" />
        </div>
    </div>
    <div class="form-group row">
        <label for="firstName" class="col-sm-2 col-form-label">
            Desc
        </label>
        <div class="col-sm-10">
            <InputText id="firstName" class="form-control" placeholder="First Name"
                       @bind-Value="StudentModelObj.Description" />
            <ValidationMessage For="@(() => StudentModelObj.Description)" />
        </div>
    </div>
    <button class="btn btn-primary" type="submit">Submit</button>
</EditForm>



@code {

    void Navigate()
    {
        UriHelper.NavigateTo("~/r1page");
        int x = 0;
    }
    private string _childData = "inside child";
    void changeAge()
    {

    }
}